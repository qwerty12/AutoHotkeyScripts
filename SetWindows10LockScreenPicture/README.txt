This is a script that uses the raw RT COM API directly to change your (i.e. the logged on user) Windows 10 lockscreen background picture. No dependencies other than Windows 10 and AutoHotkey itself.

For something more robust, you might consider https://github.com/Sauler/PowershellUtils instead.